# RAGAS 평가 전용 의존성
# 용도: ai_worker/rag/ragas_eval.py, manual_eval.py 실행 시에만 사용
# 운영 서버와 무관 — 로컬 평가 전용 venv(.venv-ragas)에서만 설치
#
# 설치:
#   python3 -m venv .venv-ragas
#   source .venv-ragas/bin/activate
#   pip install -r requirements-ragas.txt
#
# 실행:
#   source .venv-ragas/bin/activate
#   PYTHONPATH=. python ai_worker/rag/ragas_eval.py

ragas==0.1.21
datasets
openai
langchain-openai
langchain==0.2.17
langchain-community==0.2.17